<script setup>
defineProps({ status: Object })
</script>
<template><section class="page-content"><div class="page-title"><span class="eyebrow">ADMINISTRATION</span><h1>系统设置</h1><p>查看当前运行配置。服务级参数由部署环境统一管理。</p></div><div v-if="status" class="metric-grid"><article><span>同时处理对话</span><strong>{{ status.max_concurrent_chats }}</strong><small>单进程容量</small></article><article><span>检索候选数</span><strong>{{ status.retrieval_k }}</strong></article><article><span>上下文数量</span><strong>{{ status.context_k }}</strong></article><article><span>最大检索轮数</span><strong>{{ status.max_iterations }}</strong></article><article><span>模型调用上限</span><strong>{{ status.max_model_calls }}</strong></article></div><div v-if="status" class="settings-summary"><h2>服务状态</h2><p>Neo4j：{{ status.neo4j }} · Redis：{{ status.redis }} · 数据库：{{ status.database }}</p><p>并发对话上限：{{ status.max_concurrent_chats }} · 单次任务超时：{{ status.request_timeout }} 秒 · 每账户每分钟请求上限：{{ status.requests_per_minute }}</p><p class="muted">每个账户同一时间只处理一个问题；最多允许 {{ status.max_concurrent_chats }} 个不同用户同时处理。超过容量时返回繁忙提示，不无限排队。</p><h2>模型接口允许列表</h2><p v-for="url in status.user_model_base_urls" :key="url">{{ url }}</p><p class="muted">新增接口需修改 MED_USER_MODEL_BASE_URLS 并重启后端。每个账户的回答和校验使用该账户自己的模型配置。</p><h2>权限范围</h2><p>普通用户使用对话、历史记录、引用资料与个人模型设置。管理员额外查看知识库、当前账户的四层记忆、召回过程、评测及运行配置。</p><p class="notice">管理员不会自动获得其他用户的对话或记忆访问权限。</p></div></section></template>
