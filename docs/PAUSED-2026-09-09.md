# 用户要求暂停 · 2026-09-09

用户要求在额度耗完前汇报并暂停。停止继续测试、修改和外部模型调用，等待明确恢复指令。

## 已完成

- 聊天正文去掉探索模式前言，统一最后一句免责声明；用户消息靠右、助手靠左；证据面板默认收起，可展开与关闭。前端构建及桌面/390px 手机宽度浏览器检查完成。
- 前端流式协议测试 3 项通过；当前用户修改后版本的后端本地回归 57 项通过。
- 旧版 72 例真实模型测试完成，自动检查 68/72；这是旧版本开发回归，不能当作当前版本或临床准确率。
- 为用户最新检索/追问改动建立 16 个场景、32 轮口语化连续对话，经过 ChatService 与 PostgreSQL 实际持久化。离线预览和真实 GLM/OpenCode Go 两批均完成，原始报告保留。
- 此批开始时 Redis/Neo4j 未连通，中途用户启动服务，后续 Redis 能恢复；Neo4j 驱动需要重新初始化。因此该批是启动时降级、后段部分恢复的记录，不是全程在线对照。
- 用户启动后已确认 Redis connected、Neo4j connected，Neo4j 只读样例返回 1 条边。
- 故障注入发现向量运行异常未回退 BM25；重排异常可退回规则排序但缺少运行时告警；Neo4j/Redis 的模拟降级路径可用。

## 原始结果位置

- reports/industrial-dialogues.json：32 轮真实模型对话，包含完整输出、记忆窗口、来源、执行记录、耗时及版本指纹。
- reports/industrial-preview.json：同题离线预览。
- reports/service-faults.json：四类依赖故障注入结果。
- reports/answer-quality.json：旧版 72 例结果。
- docs/industrial-assessment-2026-09-09.md：已写入验收方法和阈值，最终逐条评判尚未完成。

## 已观察到的主要问题

- 口语危险描述（胸口压迫伴冷汗、嘴角歪且手抬不起来）未进入预期急救分支，出现缺资料答复。
- 记忆主要恢复实体名称，不能可靠保留时长、年龄、孕期、患者身份、否定与纠正信息；咖啡与前轮失眠未关联，甲状腺检查正常后偏到甲状腺肿大。
- 部分症状问题虽然有来源，但生成/审核失败后无法给出有帮助的回答；自动契约通过不代表用户目标已满足。
- 检索范围扩大已经落地，但不能据此认定主题理解、上下文推理或医疗安全达到上线要求。

## 恢复后待办（未执行）

1. 全程 Redis/Neo4j 在线重新运行同题，使用独立输出 industrial-online；不要覆盖混合状态旧报告。
2. 逐条审阅固定版本 32 轮，按相关性、上下文、实用性、证据支持及风险分级记录，不以自动关键词替代医学判断。
3. 更新当前版本检索消融基线，完成最终工业化评判及上线阻碍清单。
4. 该阶段用户要求测试与评判，暂不继续改动其最新检索和记忆实现；任何后续修复保留前后对照。

当前没有完成临床专家独立评审、长期负载/SLO 或公网部署验证，不得宣称可供真实患者正式医疗咨询上线。
